As tecnologias que serão utilizadas são:

Hardware: ESP32 (microcontrolador) e Sensores piezoelétricos (leitura da pressão);
Back-End: Mosquitto Broker (servidor), Flask Backend (web framework) e SQLite (banco de dados);
Front-End: React Native (framework), ExpoGo (ambiente de testes) e TypeScript (linguagem).
